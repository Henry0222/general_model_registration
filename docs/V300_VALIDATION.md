# 3.0 验证记录

## 界面与数值明细

2026-09-15，桌面界面按用户要求默认自动选择（实验），API 默认仍为原版。主界面采用左右布局、高级参数折叠、候选数值切换与模型用时。原版保留、人工选区优先及质量失败回退继续生效；科学默认启用标准未因界面设置改变。

- 116 项回归通过，0 失败、错误或跳过，最后一次本地运行 21.898 秒。
- 覆盖实际 GUI 请求模式、候选文件对应数值、缺失 / 非有限值、失败候选、路径边界和运行时控件锁定。
- 使用已有真实网格归档结果检查各候选数值；1240×820、1160×860、920×720 界面已视觉复核，基本控件及数值未被裁切。
- 启动界面仍不导入 NumPy / Open3D。
- 从已封存逐例数据独立重算 480 项统计，最大差异 1.11e-16；1,124 个计划状态全部保留，失败分母未改变。具体误差见 [数值报告](V300_METRICS.md)。

在当前源码的全新检出中复现软件回归：

```powershell
python -m pip install -e ".[dev]"
python -m pytest
```

## 几何内核移植

2026-09-15T13:05:34+08:00 完成原始移植封存；后续仅调整界面、结果文件读取和文档，几何内核哈希未改变。

- 原始 113 项回归通过；两例网格分别运行 baseline/A/B/compare/auto，10 次端到端检查通过。
- 1,090 个可计算状态、5,450 组矩阵与封存原型一致，最大矩阵差异 0。
- 34 个不可计算状态（native/gold 各 17 个）继续计入严格成功分母。
- 独立重算连续误差、严格成功、逐例改善 / 退化和正确初值保持，均与原封存审计一致；连续指标最大差异 3.44e-14。
- 48 个原有配准函数体保持不变；原始移植快照的 wheel 在源码目录之外导入并执行 A/B 的验证通过。该 wheel 检查不覆盖之后的 UI 改动。

这些检查证明移植行为一致，不代表新的产品端到端泛化结果。自动路线仍有正确初值退化，保留实验提示与原版复核入口。

## 完整产品耗时

其他数值计算结束后，对两例预先选定的大网格串行测量；包含加载、前级、几何后级、候选质量复核及全部导出。两例采用相反模式顺序，文件缓存已热，排除 Python 启动。

|输入标识|原版秒数|auto 秒数|增加|
|---|---:|---:|---:|
|1841618b0cd311159141|49.13|91.54|86.3%|
|72670e8376db0ca70373|42.94|70.44|64.1%|

这些仅是本机两例大网格的使用成本示例，不是所有模型或硬件的耗时保证。原型 10 例的 +26.0% 测量不含此处完整的质量复核与导出成本，不能与本表混用。

## 证据与复现范围

本地冻结归档 `local_v300_20260915T113844` 保留矩阵、逐例指标、来源 / 折统计、全部失败、运行时钟、进程登记与复现入口；`v300_ui_auto_20260915T132159` 保留当前 UI 测试、截图与连续统计重算。两份归档均已通过逐文件 SHA-256 核验。

本 PR 包含源码、合成单元测试及汇总报告；原始研究网格、训练缓存与上述完整归档不在仓库中。因此软件回归可直接复现，全量研究面板复核需要对应冻结数据，不能仅靠本 PR 独立复现。

本版本没有构建或发布 EXE。功能与边界见 [V300_REFINEMENT.md](V300_REFINEMENT.md)。
