# Changelog

本项目遵循语义化版本编号。

## 1.4.1 - 2026-08-30

- 质量门控失败但已得到有限变换时，保存并允许查看明确标记为“仅供检查”的最佳候选。
- 在固定模型和每个浮动模型后增加独立的三维查看与自由套索选区编辑器。
- 支持可见面选择、透选、Ctrl 套索取消、全选、取消、反选和有界组件扩选。
- 支持仅影响工作副本的面片删除，以及按表面积保留最大主组件的浮动面片清理。
- 有选区时进行全模型安全基线与选区优先候选双通道评估，通过独立选区门控和全模型安全检查决定采用或回退。
- 恢复无选区采样的 v1.4.0 原始数值路径，固定 FGR 初始化状态和核心运行库版本，避免打包环境改变粗配准盆地。
- 修复无控制台打包版在 Open3D 输出 RANSAC 诊断时偶发 `NoneType.write` 导致单项失败。
- 修复模型选区窗口中文字体缺字、自由套索轨迹被模型遮挡、切换模型后编辑计数残留，以及透选遗漏与套索相交面片的问题。
- 将自由套索改为真正的 3 像素屏幕空间带状面片，不再依赖 Windows/Open3D 后端可能忽略的硬件线宽；非透选改为逐面中心连续可见性判断，并拆分底模与选中面渲染，消除选区的三角形漏选和深度冲突斑驳。
- 增加可选的“彻底检查可能朝向”：粗配准阶段枚举 24 个合法 PCA 轴姿态，对近似轴对称模型补充绕轴候选，双向表面评分后只让优选姿态进入完整多尺度 ICP；默认关闭，不改变原有快速配准路径。

## 1.4.0 - 2026-08-29

- 增加一个固定模型与最多 50 个浮动模型的顺序批处理。
- 增加资源管理器拖放和逐模型面朝向/法线翻转。
- 输出改为 `align_MMDDHHmm`，归档实际固定模型并生成批次及逐项日志。
- 增加失败隔离、安全停止、`failure.json` 和历史记录扫描/导入。
- 恢复并开源高级 Open3D 结果查看器源码。
- 将离散 15 色改为连续插值，并增加左侧透明色标和左下角偏差说明。
- 移除新配准中的红蓝方向互换入口，同时保留旧结果读取兼容性。

## 1.3.0 - 2026-08-29

- 开放完整 Python 配准算法源码。
- 增加末级高精度 point-to-surface ICP。
- 增加覆盖率、空间分布、可观测性和最大位移门控。
- 高精度候选不可靠时自动回退到多尺度 ICP 结果。
- 在 `results.json` 中记录高精度决策和质量指标。
- 提供可复现的 Windows 构建、测试和发布流程。
