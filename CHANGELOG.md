# Changelog

本项目遵循语义化版本编号。

## 3.0.0 - 2026-09-15（源码版）

- 桌面界面默认自动选择；公共 API 保持 2.0 默认计算路径。新增 A 局部共同表面、B 连续表面偏差、A/B 对比和实验性自动选择。
- 主界面分区、高级参数折叠；结果表增加 P95 和耗时，所选候选显示 RMS、平均距离、中位数、P95 与最大距离。
- 修复英文 Windows 环境中 Open3D 对中文路径静默读写失败时未回退的问题；保留 PLY 颜色，导出失败时保护原文件并清理临时文件。
- 配准结果 STL 保留输入时的面朝向：计算中启用的法线翻转在导出副本上撤销，配准位置和删面结果保留；主结果、A/B 候选与失败预览统一处理，结果查看仍使用计算方向。
- 主界面右下角增加 Henry Van 署名和可点击的 GitHub 仓库链接。
- A9 与 smooth17 几何内核移入正式包，运行时不依赖研究目录、学习模型或 PyTorch；只接受网格和初始刚体矩阵。
- 原版位姿保留供复核，候选各自导出矩阵、色图与质量报告；更新 GUI、批次日志和公共 API。
- 人工选区与原版失败优先；精修异常或候选质量失败时保留原版并记录原因。阶段间支持协作式取消。
- 新增 SciPy 1.18.1 运行依赖，运行与分发配置统一采用 Python 3.12。未构建 EXE，未发布。
- 自动路由仍有已正确位姿退化；桌面默认值按用户要求调整，不表示已经通过科学默认启用标准。既有研究结果与工程迁移验证分开报告，见 `docs/V300_REFINEMENT.md`。

## 2.0.0 - 2026-09-05（源码验证版）

- 2026-09-08：RANSAC 在同次配准内复用各尺度的双向特征最近邻，保留 Open3D 的匹配顺序、互选过滤及小集合回退规则；复用已有尺度点云，保留全部搜索重启、采样与精化参数。进度新增逐次全局匹配提示，候选诊断新增匹配/搜索耗时与搜索次数。性能及位姿复核见 `docs/V200_PERFORMANCE.md`。

- 2026-09-06：有金标准的离线评价改以六自由度与完整矩阵为主，区分平移列差、固定坐标残余位姿和校正矩阵；恢复原实验输入参数，P95 降为辅助。旧/新算法与 Wrap 的既有矩阵已重评，不重新配准或改变已选结果。新增 9 项矩阵评价测试通过。

- 默认使用新的候选池引擎，保留全局、粗、中、精各阶段姿态；所有候选按同一套双向曲面采样、分母、多尺度距离和空间覆盖指标评价，精化未带来可辨别改善时保留父姿态。
- 增加多尺度 FPFH / FGR / RANSAC、合法 PCA 轴组合、平面结构候选、分支式共同表面精化及 GICP 候选。固定评价同时检查完整最近曲面距离、法向一致性与六自由度可观测性。
- 在数据对内缓存采样、特征和两个最近曲面查询场景，保留原面片索引及重心坐标。修复 PCA 反射基、坐标原点影响平移门控、噪声估计重纳入已排除点的问题。
- 残差色图使用完整欧氏最近曲面距离，目标法向仅决定符号，避免开放边界切向偏差显示为零。
- 窗口与计算引擎解耦，首次配准在后台加载数值依赖。本机三次新进程、热文件缓存、离屏 Qt 测试中，窗口就绪中位时间 1.483 s → 0.259 s。
- 增加可复现的 18 组数据测试、候选诊断、源码启动分析与结果导出脚本。实验真值只在配准结束后用于误差计算，不进入生产候选选择。
- 未构建 EXE。已知限制：严重表面改变仍存在候选评分偏差；并非所有实验优于旧版或 Wrap。详细结果见 `docs/V200_TEST_REPORT.md`。

## 先前未发布的 1.4.x 开发记录（保留历史）

以下是本轮改造前的工作记录；2.0 默认路径已由候选池引擎替代，不能作为本次 2.0 的测试结论。

### 配准算法

- 末级精配准改为自适应稳定区模式：从残差自动估计噪声 σ，在面片邻接图上平滑出连贯的稳定区，用局部均值检测隐藏在噪声内的大面积小幅变化（牙龈肿胀、打印收缩），并只用稳定区拟合。位姿与稳定区交替迭代两轮。
- 稳定区在空间上分成交错的两半，只拟合一半，在另一半和固定模型上验证中位误差、P90、法向偏置、一致面积和最大位移；未通过验证时保留多尺度 ICP 结果并记录原因。原固定阈值门控可通过 `stable_region_enabled=False` 启用。
- 合成基准（`scripts/synthetic_benchmark.py`，真实扫描 + 已知变换 + 局部/大面积/缩放/噪声扰动）显示：干净与局部变化情形位姿误差从 3–5 µm 降到约 1 µm；牙龈大面积小幅变化情形从 39 µm 降到 4–9 µm；立方体测试集中“面3变化”从 34 µm 降到 0.7 µm，其余情形持平。
- 修复质量评估中候选一致性被末级诊断项掩盖的问题：现在只有独立初始化候选参与一致性投票，模糊姿态会如实降低可信度。
- 高精度 ICP 收敛判据从永远不会触发的 1e-11 放宽到亚微米级，避免固定跑满迭代上限。
- `results.json` 与 `registration.log` 新增稳定区诊断：σ、阈值、初始/精配准后稳定区面积占比、留出区前后误差、最大位移。

### 界面

- 记住上次使用的结果根目录，下次启动自动填入（目录不存在时回退默认值）。
- 主窗口视觉重排：标题区、卡片式分组、双列参数表、主操作按钮、细进度条与交替行结果表。
- 批次失败对话框保留完整技术堆栈（“详细信息”可展开）并写入错误日志。
- 既往记录扫描移到后台线程，且只扫描两级目录，避免历史积累后卡住界面。
- 同一模型的选区编辑器不再允许重复打开；编辑器进程改挂在应用上，关闭主窗口不再瞬间杀掉所有 3D 窗口；启动失败时正确释放。
- 关闭时清理本会话生成的临时选区状态文件。

### 三维查看器与选区编辑器

- 右键弹出菜单不再重建整个网格；选区变化时复用工作副本，只有删除面片才重建。
- 套索丝带改为向量化生成并批量反投影，射线场景缓存，可见面选择先按套索包围盒预筛；24 万面模型上一次套索从约 1 s 降到约 0.2 s。
- 结果查看器从 7 份网格副本减到 3 份，叠加模式通过材质颜色切换。
- 标注写入改为原子写入并在只读目录下给出提示而不是异常；成对查看模式不再向用户目录写入或读取标注文件。
- 编辑器关闭 0.75 s 内的延迟回调不再投递到已销毁窗口。
- 连通组件标记改为向量化并查集，24 万面模型从 1.0 s 降到 0.13 s。

## 1.4.2 - 2026-09-01

- 将选区从软采样提示升级为操作者主导的配准目标：选区覆盖率、中位误差和 P90 优先决定最终变换，完整模型仅用于粗定位和灾难性错位检查。
- 保留 70% 选区加权全局候选并增加严格选区全局候选，分别从全模型粗定位、加权候选和严格候选执行仅使用选中面片的局部精配准。
- 选区覆盖率低于可信阈值时不再强制回退全模型，而是采用选区最优安全候选并降低可信度、写入明确警告。
- 对面片过少或无法约束六自由度的选区保留安全回退，并明确报告具体原因。
- 结果表新增“配准依据”，日志记录最终选区指标、采用通道、选区几何有效性和低覆盖率降级状态。
- 兼容读取 v1.4.1 保存的模型选区与工作副本编辑状态。

## 1.4.1 - 2026-08-30

- 质量门控失败但已得到有限变换时，保存并允许查看明确标记为“仅供检查”的最佳候选。
- 在固定模型和每个浮动模型后增加独立的三维查看与自由套索选区编辑器。
- 支持可见面选择、透选、Ctrl 套索取消、全选、取消、反选和有界组件扩选。
- 支持仅影响工作副本的面片删除，以及按表面积保留最大主组件的浮动面片清理。
- 有选区时进行全模型安全基线与选区优先候选双通道评估，通过独立选区门控和全模型安全检查决定采用或回退。
- 恢复无选区采样的 v1.4.0 原始数值路径，固定 FGR 初始化状态和核心运行库版本，避免打包环境改变粗配准盆地。
- 修复无控制台打包版在 Open3D 输出 RANSAC 诊断时偶发 `NoneType.write` 导致单项失败。
- 修复模型选区窗口中文字体缺字、自由套索轨迹被模型遮挡、切换模型后编辑计数残留，以及透选遗漏与套索相交面片的问题。
- 将自由套索改为真正的 3 像素屏幕空间带状面片，不再依赖 Windows/Open3D 后端可能忽略的硬件线宽；非透选改为逐面中心连续可见性判断，并拆分底模与选中面渲染，消除选区的三角形漏选和深度冲突斑驳。
- 增加可选的“彻底检查可能朝向”：粗配准阶段枚举 24 个合法 PCA 轴姿态，对近似轴对称模型补充绕轴候选，双向表面评分后只让优选姿态进入完整多尺度 ICP；默认关闭，不改变原有快速配准路径。

## 1.4.0 - 2026-08-29

- 增加一个固定模型与最多 50 个浮动模型的顺序批处理。
- 增加资源管理器拖放和逐模型面朝向/法线翻转。
- 输出改为 `align_MMDDHHmm`，归档实际固定模型并生成批次及逐项日志。
- 增加失败隔离、安全停止、`failure.json` 和历史记录扫描/导入。
- 恢复并开源高级 Open3D 结果查看器源码。
- 将离散 15 色改为连续插值，并增加左侧透明色标和左下角偏差说明。
- 移除新配准中的红蓝方向互换入口，同时保留旧结果读取兼容性。

## 1.3.0 - 2026-08-29

- 开放完整 Python 配准算法源码。
- 增加末级高精度 point-to-surface ICP。
- 增加覆盖率、空间分布、可观测性和最大位移门控。
- 高精度候选不可靠时自动回退到多尺度 ICP 结果。
- 在 `results.json` 中记录高精度决策和质量指标。
- 提供可复现的 Windows 构建、测试和发布流程。
